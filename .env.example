# Claude API Key for meme caption generation
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Search engine mode:
# - auto: full locally, lexical on Railway free tier (recommended)
# - full: full multimodal CLIP (higher memory)
# - text: CLIP text embeddings only (lower memory)
# - lexical: keyword fallback only
SEARCH_MODE=auto

# Optional: cap templates fetched from each remote source (imgflip/memegen).
# Lower values reduce startup memory and indexing time.
MAX_REMOTE_MEMES=160
